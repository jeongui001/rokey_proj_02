# 팀과 공유하지 않는 개인 override 자리 표시자 - 기본값은 비어있어 아무것도 덮어쓰지 않는다.
# 개인 값은 이 파일을 직접 수정하지 않고 별도 파일로 관리한다:
#   1. dev/ 아래(gitignore됨) 개인 override 파일 작성. 예: dev/local_params.yaml
#        /**:
#          ros__parameters:
#            doosan_driver:
#              controller_name: 'dsr_controller2'
#   2. launch 시 local_params_file 인자로 경로 전달:
#        ros2 launch task_manager handover_system.launch.py \
#          local_params_file:=/home/rokey/rokey_proj_02/dev/local_params.yaml
/**:
  ros__parameters: {}
